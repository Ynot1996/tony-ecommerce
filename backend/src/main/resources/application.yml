spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tony_ecommerce
    username: postgres
    password: your_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    mongodb:
      uri: mongodb://localhost:27017/tony_ecommerce

server:
  port: 8080

jwt:
  secret: your_jwt_secret_key
  expiration: 86400000 