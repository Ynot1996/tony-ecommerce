## 分支策略

本專案採用 Git Flow 分支策略：

- `main`: 產品主分支，只接受來自 `develop` 的合併
- `develop`: 開發主分支，所有功能開發完成後合併到此
- `feature/*`: 功能分支，用於開發新功能
- `hotfix/*`: 緊急修復分支，用於修復線上問題
- `release/*`: 發布分支，用於版本發布前的準備

### 開發流程

1. 從 `develop` 分支建立新的 `feature` 分支 