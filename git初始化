# 初始化 Git 倉庫
git init

# 添加文件到暫存區
git add .

# 創建第一個提交
git commit -m "初始化專案：
- 建立前端 React 專案結構
- 建立後端 Spring Boot 專案結構
- 設定基本依賴和配置文件"

# 建立主分支（如果需要）
git branch -M main

# 如果您要連接到遠端倉庫，可以執行：
# git remote add origin your-repository-url
# git push -u origin main 